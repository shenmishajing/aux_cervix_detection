__base__: schedule_1x.yaml

model:
    init_args:
        optimizer_config:
            lr_scheduler:
                scheduler:
                    milestones: [ 16, 19 ]

trainer:
    init_args:
        max_epochs: 20
